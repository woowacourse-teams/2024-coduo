import styled from 'styled-components';

export const Body = styled.div`
  margin: 4rem 0;
`;

export const Footer = styled.div`
  display: flex;
  gap: 1rem;

  position: absolute;
  right: 4rem;
  bottom: 4rem;
`;

import styled from 'styled-components';

export const Layout = styled.div`
  position: fixed;
  z-index: 9999;
  top: 9rem;
  right: 2rem;

  display: flex;
  flex-direction: column;
  gap: 0.6rem;
`;
